import json
with open('/root/books/webapp/books.json') as f:
    data = json.load(f)

r2 = {
    # 249 Робинзон Крузо
    (249,1): ' Дефо написал первую книгу о нарративной терапии — за 270 лет до того как психологи дали ей имя.',
    (249,2): ' Дефо показывает: человек не просто выживает — он растёт. Возможность всегда существует.',
    (249,3): ' Инструменты — возможность превратить мысль в действие. Без них невозможно мыслить как цивилизованный человек.',
    (249,5): ' Переключить внимание с того чего нет на то что есть — когнитивный инструмент а не самообман.',
    (249,6): ' Смысл находится в процессе создания — не в достижении цели. Крузо доказывает это каждым днём.',
    (249,7): ' Этот сдвиг от аллегории к реализму изменил всю последующую литературу.',
    (249,8): ' Возвращение как утрата — тема которую Дефо только наметил а последующая литература развила.',
    (249,9): ' Книга больше любого прочтения — именно это делает её живой через 300 лет.',

    # 250 Путь паломника
    (250,3): ' Один человек который помогает двигаться вперёд стоит дороже десятка которые тянут назад. Это не снобизм — самосохранение.',
    (250,4): ' Ярмарка Тщеславия описывает не XVII век — а механизм который воспроизводится в каждую эпоху.',
    (250,5): ' Первый шаг к свободе — не достижение цели, а освобождение от того что мешает идти.',
    (250,6): ' Буньян расширил мир читателей — дав имена тому что переживали но не умели назвать.',
    (250,7): ' Конкретность — не ограничение. Это способ достучаться до универсального через частное.',
    (250,8): ' Жизнь с ясной целью — качественно иная жизнь. Это работает для любого читателя.',
    (250,9): ' Его простота — не упрощение. Это демократизация смысла. Именно поэтому его читали везде.',

    # 253 Маленькие женщины
    (253,8): ' Испытание как инструмент роста — не всегда, но при наличии поддержки очень часто.',
}

for book in data:
    bid = book['id']
    for i, t in enumerate(book['thoughts']):
        if (bid, i) in r2:
            t['example'] = t['example'] + r2[(bid, i)]

with open('/root/books/webapp/books.json', 'w') as f:
    json.dump(data, f, ensure_ascii=False)

# Verify
ids = [249, 250, 253]
for book in data:
    if book['id'] in ids:
        lens = [len(t['example']) for t in book['thoughts']]
        bad = [(i, l) for i, l in enumerate(lens) if l < 700 or l > 820]
        status = 'OK' if not bad else f'BAD={bad}'
        print(f'ID={book["id"]} {book["title"][:22]}: min={min(lens)} max={max(lens)} {status}')
